target
.*sw?
.env
core.*
docker-target

node_modules
# anchored to repo root so `ui/package.json` etc are NOT ignored. the root
# package.json/package-lock.json exists for `npx @devcontainers/cli`, driven
# by `just container`, and is the only one we don't want tracked.
/package.json
/package-lock.json
/pnpm-lock.yaml

/research/*
!/research/mitm/
/research/mitm/*
!/research/mitm/capture.py
.direnv/

/addon-dev/

/iot-trace.log
